import { createTheme } from '@mui/material/styles';

const themeOptions = {
  palette: {
    mode: 'dark',
    primary: {
      main: '#3fb5a4',
    },
    secondary: {
      main: '#66f500',
    },
  },
};

export default createTheme(themeOptions);
  